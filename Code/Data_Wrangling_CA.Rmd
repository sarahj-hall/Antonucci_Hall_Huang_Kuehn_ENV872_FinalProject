---
title: "Initial_Data_Wrangling"
output: pdf_document
---

```{r setup, include=FALSE}
library(tidyverse)
library(lubridate)
library(here)
```

```{r load data}
outage_group_CA <- read.csv(here('Data/Processed/outage_group_CA.csv'), 
                            stringsAsFactors = TRUE)
#read in CA data from processed folder 
#change datas?
#remove unwanted data columns  
#remove 0 month?

```

```{r process into 4 state files}
#filter out for california and export to processed data file 
#processing: plots, time plot, county spatial plot? 

#line plots for each state yearly outages from 2015-2023
outage_group_CA %>% 
  filter(month == 0) %>% 
  ggplot(aes(x = year,
      y = outage_count))+
  geom_point()+
  geom_line()

#line plots for each state, monthly 
outage_group_CA %>% 
  filter(month != 0) %>% 
  ggplot(aes(x = month,
      y = outage_count))+
  geom_point()+
  geom_line()


```

